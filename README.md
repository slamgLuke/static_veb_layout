# Static vEB Layout — I/O Benchmark

## Project focus

A static implementation of the **van Emde Boas tree layout** — a cache-oblivious
layout for a complete binary search tree of `n = 2^h - 1` keys. The tree is
built once over a sorted input; nodes carry explicit `left` / `right` indices
into a flat node array, so traversal does not depend on heap-style implicit
indexing. The hypothesis under test is the classical one:

- vEB performs `O(log_B n)` block transfers per query, **without** knowing `B`
  or `M` at construction time.
- A flat-array binary search performs more block transfers in practice because
  its mid-point probes near the top of the tree are scattered across distinct
  blocks at every level.

The static layout is built once and then queried; we are not measuring
construction cost, only query I/O and query wall time.

## Testing model

We do not rely on real CPU cache counters. Instead we drive both algorithms
through an explicit **LRU cache simulator** (`cache.h` / `cache.cpp`) so the
I/O count is deterministic and independent of the host machine:

- The cache is parameterized by **block size `B`** (in elements) and
  **capacity `M`** (in blocks).
- Every node access `arr[i]` is routed through `cache.access(i)`.
- Each access maps to block `i / B`, refreshes the block's last-used
  timestamp, and increments a miss counter on a fault.
- Eviction is exact LRU over a `block → last-access timestamp` map.
- The cache is **shared across all queries** within a configuration — it is
  not reset between queries, so warm-cache locality across queries is part of
  the measurement.

The flat-array baseline `flat_search` is a textbook binary search over an
`iota(1..n)` array; every `arr[mid]` access goes through the same cache, so
both algorithms are scored on the same metric: number of LRU misses.

Queries are generated by `mt19937(42)`, re-seeded per `(x, B)` configuration
so the vEB and flat-array runs receive the **identical** query stream within
each cell.

We also report **wall-clock time** per configuration. The timing wraps the
full search loop, including `cache.access` and the LRU simulator's eviction
scan, so absolute times reflect *simulator + algorithm*, not the algorithm
alone — the eviction scan is `O(M)` and dominates on large caches. The
relative vEB-vs-flat comparison is still meaningful because both algorithms
pay identical simulator cost per access.

The `test.cpp` suite covers four checks:

- `test_build_correctness` — hand-traced layouts for `h ∈ {2, 3, 4}`.
- `test_search_correctness` — every present key found, absent keys missing.
- `test_io_counting` — hand-traced miss counts on a small tree.
- `test_stress_correctness` — randomised distinct sorted keys for
  `h ∈ [1, 10]`, verifying both presence and absence under noise.

## Fixed parameters

| Parameter        | Value                                              |
| ---------------- | -------------------------------------------------- |
| Tree height `h`  | `20`                                               |
| Number of keys   | `n = 2^20 - 1 = 1,048,575`                         |
| Sorted input     | `iota(1..n)`                                       |
| Query RNG        | `std::mt19937(42)`, reseeded per `(x, B)`          |
| Query domain     | `rng() % (2n)` (mix of hits and out-of-range)      |
| Block sizes `B`  | `{4, 8, 16, 32, 64, 128, 256}` (elements)          |
| Cache `M`        | `factor · B` blocks; `factor = 8` for `B ≤ 32`, `factor = 4` for `B ≥ 64` |
| Query counts     | `q = 10^x` for `x ∈ {1..6}`                        |
| Compiler         | `g++ -O2`                                          |
| Clock            | `std::chrono::high_resolution_clock`               |

## Results

Each cell reports the total number of LRU misses and the wall-clock time
across all `q` queries for that configuration.

### `q = 10^1`

| B   | M    | vEB misses | vEB ms | flat misses | flat ms |
| --- | ---- | ---------- | ------ | ----------- | ------- |
| 4   | 8·B  | 77         | 0.019  | 165         | 0.034   |
| 8   | 8·B  | 51         | 0.005  | 116         | 0.014   |
| 16  | 8·B  | 42         | 0.004  | 100         | 0.006   |
| 32  | 8·B  | 30         | 0.003  | 94          | 0.005   |
| 64  | 4·B  | 27         | 0.003  | 86          | 0.005   |
| 128 | 4·B  | 23         | 0.002  | 78          | 0.004   |
| 256 | 4·B  | 19         | 0.002  | 70          | 0.004   |

### `q = 10^2`

| B   | M    | vEB misses | vEB ms | flat misses | flat ms |
| --- | ---- | ---------- | ------ | ----------- | ------- |
| 4   | 8·B  | 647        | 0.079  | 1,319       | 0.181   |
| 8   | 8·B  | 379        | 0.062  | 774         | 0.121   |
| 16  | 8·B  | 252        | 0.051  | 629         | 0.139   |
| 32  | 8·B  | 183        | 0.022  | 514         | 0.153   |
| 64  | 4·B  | 151        | 0.021  | 459         | 0.112   |
| 128 | 4·B  | 125        | 0.018  | 390         | 0.031   |
| 256 | 4·B  | 111        | 0.018  | 336         | 0.029   |

### `q = 10^3`

| B   | M    | vEB misses | vEB ms | flat misses | flat ms |
| --- | ---- | ---------- | ------ | ----------- | ------- |
| 4   | 8·B  | 6,700      | 0.854  | 12,460      | 1.549   |
| 8   | 8·B  | 3,783      | 0.670  | 7,607       | 1.234   |
| 16  | 8·B  | 2,557      | 0.751  | 5,975       | 1.688   |
| 32  | 8·B  | 1,588      | 0.802  | 4,799       | 2.343   |
| 64  | 4·B  | 1,268      | 0.606  | 4,192       | 1.978   |
| 128 | 4·B  | 1,032      | 0.579  | 2,989       | 2.278   |
| 256 | 4·B  | 882        | 0.172  | 1,840       | 1.538   |

### `q = 10^4`

| B   | M    | vEB misses | vEB ms | flat misses | flat ms |
| --- | ---- | ---------- | ------ | ----------- | ------- |
| 4   | 8·B  | 62,866     | 10.34  | 119,701     | 13.16   |
| 8   | 8·B  | 35,560     | 6.97   | 71,581      | 13.26   |
| 16  | 8·B  | 23,928     | 8.20   | 56,605      | 16.96   |
| 32  | 8·B  | 14,620     | 8.75   | 45,394      | 23.33   |
| 64  | 4·B  | 11,731     | 7.26   | 39,549      | 20.35   |
| 128 | 4·B  | 9,409      | 13.50  | 27,538      | 32.71   |
| 256 | 4·B  | 6,452      | 15.46  | 14,754      | 32.71   |

### `q = 10^5`

| B   | M    | vEB misses | vEB ms  | flat misses | flat ms |
| --- | ---- | ---------- | ------- | ----------- | ------- |
| 4   | 8·B  | 628,159    | 99.36   | 1,196,477   | 129.61  |
| 8   | 8·B  | 355,954    | 69.28   | 714,210     | 129.50  |
| 16  | 8·B  | 239,873    | 74.99   | 566,057     | 166.46  |
| 32  | 8·B  | 146,253    | 84.49   | 451,821     | 237.59  |
| 64  | 4·B  | 117,018    | 90.84   | 394,139     | 220.31  |
| 128 | 4·B  | 93,567     | 103.14  | 273,938     | 271.07  |
| 256 | 4·B  | 62,753     | 136.31  | 143,750     | 265.39  |

### `q = 10^6`

| B   | M    | vEB misses  | vEB ms   | flat misses  | flat ms  |
| --- | ---- | ----------- | -------- | ------------ | -------- |
| 4   | 8·B  | 6,269,076   | 831.50   | 11,935,363   | 1528.62  |
| 8   | 8·B  | 3,554,828   | 699.99   | 7,131,173    | 1326.07  |
| 16  | 8·B  | 2,393,108   | 818.78   | 5,651,777    | 1678.90  |
| 32  | 8·B  | 1,460,507   | 842.46   | 4,512,668    | 2922.82  |
| 64  | 4·B  | 1,166,295   | 786.84   | 3,937,063    | 2323.99  |
| 128 | 4·B  | 933,357     | 1141.04  | 2,731,526    | 2720.14  |
| 256 | 4·B  | 624,858     | 1290.77  | 1,434,030    | 2886.18  |

## Observations

- vEB beats flat-array binary search in **every** `(B, q)` cell, on both
  miss count and wall time.
- Per-query miss rate stabilizes very quickly: from `q = 10^3` upward the
  flat/vEB miss ratio is essentially fixed for each `B`, which says the
  cache reaches a steady state within the first few hundred queries.
- The flat/vEB miss ratio sits in `[1.86, 3.68]`. It grows with `B` up to
  the point where the cache factor drops from `8` to `4` (`B = 64`), and
  then flattens — the relative win is dominated by how much of the tree's
  top fits in the cache, which falls when we shrink the factor.
- vEB misses fall roughly geometrically as `B` grows (each doubling of `B`
  cuts misses by `~1.5×–1.7×`), consistent with the `O(log_B n)` bound.
- Wall-time ratios track miss ratios closely (1.7×–3× faster), confirming
  that the dominant per-query cost on this simulator is driven by misses.

## Reproducing

```bash
g++ -O2 cache.cpp veb.cpp main.cpp -o bench
./bench

g++ -O2 cache.cpp veb.cpp test.cpp -o test
./test
```

The benchmark is deterministic on miss counts; wall times will vary by
machine and noise.
